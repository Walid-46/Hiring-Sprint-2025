# this is the base model used to analyze images
# a better version is used in the main fast api
# this can be used to check the model detection locally

# to run:
# run the jupyter note book
# be sure to install ultralytics >> pip install ultralytics
# run the cells in the note book
# a cell output only summary
# the other saves the images in the same folder the notebook is ran

###note to change images just change the image_path variable to your image path


# the last cell can be used to test the fast api locally

# install the requirements from the requirements.txt
# then use: uvicorn main.py:app
# then run the cell to get the results
